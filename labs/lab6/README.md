# Lab 6: Conditional Statements (if/else)

## Objective
Learn to use conditional branching with `if`, `else if`, and `else`.

## Completed Tasks
- [x] Using `if` statement
- [x] Using `else if` for multiple conditions
- [x] Using `else` for fallback
- [x] Conditional logic

## Technologies Used
- HTML5
- CSS3
- JavaScript

## How to Run
1. Open `index.html` in any modern browser
2. Open the console (F12) to see results

## Author
Artur Bilous